{
  "name": "careech-admin",
  "version": "1.0.0",
  "description": "Careech Admin Panel - Secure Management Dashboard",
  "author": "Careech",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:sandbox": "wrangler pages dev dist --d1=careech-production --local --ip 0.0.0.0 --port 3001",
    "build": "vite build && mkdir -p dist/static && cp public/static/careech-logo.png dist/static/ && cp public/static/favicon.ico dist/static/",
    "preview": "wrangler pages dev dist",
    "deploy": "npm run build && wrangler pages deploy dist --project-name careech-admin",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "clean-port": "fuser -k 3001/tcp 2>/dev/null || true",
    "test": "curl http://localhost:3001/health"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "hono": "^4.11.9",
    "jsonwebtoken": "^9.0.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250705.0",
    "@hono/vite-cloudflare-pages": "^0.4.2",
    "typescript": "^5.0.0",
    "vite": "^6.3.5",
    "wrangler": "^4.4.0"
  }
}
